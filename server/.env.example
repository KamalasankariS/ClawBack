# PostgreSQL connection (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://confido:confido@localhost:5433/confido?schema=public"

# JWT secret (auto-generated if not set)
# JWT_SECRET="your-secret-here"
